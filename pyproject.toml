[project]
name = "ha-power-insight"
version = "0.1.0b1"
description = "Home Assistant custom component for electrical power insights"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
