---
title: Captive battery
description: "bat1 may only charge from pv1. That single restriction changes the answer in two opposite ways depending on whether pv1 is producing: when it is, the captive sink depletes it before anyone else may share it; when it is not, the sink has nowhere legal to draw from."
---

import {referenceCase, PROPERTIES} from './cases/all';

export const CASE = referenceCase('captive-battery');

# Captive battery

{CASE.summary}

## What this case pins down

<ul>
  {CASE.decides.map((d) => (
    <li key={d}>{d}</li>
  ))}
</ul>

<CaseDiagram case={CASE} properties={PROPERTIES} />

## The snapshots

Each card above is one snapshot of this wiring. The tabs under them re-label the
same picture with a different layer of the engine — the readings, where each
sink's power came from, how the gross power split between channels, and what it
all cost — and every value the engine published for that snapshot is listed
underneath.

{CASE.states.map((s) => (
  <div key={s.id} style={{marginBottom: '1.25rem'}}>
    <p style={{marginBottom: '0.4rem'}}>
      <code>{s.id}</code> — {s.note}
    </p>
    {s.open_question && (
      <blockquote>
        <strong>Open question.</strong> {s.open_question}
      </blockquote>
    )}
  </div>
))}

## Why this case exists

One restriction, two opposite consequences — which is why both snapshots share a
wiring.

When the captive battery's allowed source is producing, the battery **depletes
it first**: it takes all of `pv1`, and the unrestricted consumer is left with
the grid. A naive implementation that shared each source proportionally would
hand the battery a slice of grid power it is not allowed to touch, so this
snapshot separates the two implementations immediately.

When that same source drops into standby it stops being a source at all. The
battery now has nowhere legal to draw from, and rather than dividing by zero the
row collapses to zeros and the shortfall is reported as a **restriction
deficit**.
