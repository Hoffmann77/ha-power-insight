---
title: Baseline mix
description: "The degenerate baseline every other case is read against. Nothing is restricted, so each sink simply mirrors the raw availability of the sources, and the unmetered home load competes for power like any other sink."
---

import {referenceCase, PROPERTIES} from './cases/all';

export const CASE = referenceCase('baseline-mix');

# Baseline mix

{CASE.summary}

## What this case pins down

<ul>
  {CASE.decides.map((d) => (
    <li key={d}>{d}</li>
  ))}
</ul>

<CaseDiagram case={CASE} properties={PROPERTIES} />

## The snapshots

Each card above is one snapshot of this wiring. The tabs under them re-label the
same picture with a different layer of the engine — the readings, where each
sink's power came from, how the gross power split between channels, and what it
all cost — and every value the engine published for that snapshot is listed
underneath.

{CASE.states.map((s) => (
  <div key={s.id} style={{marginBottom: '1.25rem'}}>
    <p style={{marginBottom: '0.4rem'}}>
      <code>{s.id}</code> — {s.note}
    </p>
    {s.open_question && (
      <blockquote>
        <strong>Open question.</strong> {s.open_question}
      </blockquote>
    )}
  </div>
))}

## Why this case exists

Every other reference case is a departure from this one. Nothing here is
restricted: each battery and consumer may draw from the whole mix, so each
sink's provenance row is simply the raw availability of the sources. If the
tiered logic in the solver is working, this is what it must collapse to when
there is nothing to tier.

It is also where the **home base load** is easiest to see. The metered devices
do not account for all the power entering the house; the remainder is drawn as a
dashed circle and competes for power exactly like a real device.
