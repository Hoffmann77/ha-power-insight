---
title: PV export
description: "The same two devices, with the string now permitted to export. Reversing the grid changes its kind rather than its sign: it stops being a source and becomes a sink, which is all it takes to switch on the export channel and its compensation."
---

import {referenceCase, PROPERTIES} from './cases/all';

export const CASE = referenceCase('pv-export');

# PV export

{CASE.summary}

## What this case pins down

<ul>
  {CASE.decides.map((d) => (
    <li key={d}>{d}</li>
  ))}
</ul>

<CaseDiagram case={CASE} properties={PROPERTIES} />

## The snapshots

Each card above is one snapshot of this wiring. The tabs under them re-label the
same picture with a different layer of the engine — the readings, where each
sink's power came from, how the gross power split between channels, and what it
all cost — and the values derived for that snapshot are listed underneath. Only
derived values appear, so a short table means the hand-checking has not got far
here; a snapshot whose provenance is still undecided has no flows to draw.

{CASE.states.map((s) => (
  <div key={s.id} style={{marginBottom: '1.25rem'}}>
    <p style={{marginBottom: '0.4rem'}}>
      <code>{s.id}</code> — {s.note}
    </p>
    {s.open_question && (
      <blockquote>
        <strong>Open question.</strong> {s.open_question}
      </blockquote>
    )}
  </div>
))}

## Why this case exists

Nothing is added here. The same two devices are wired the same way, with one
configuration flag flipped, and that is the point — the whole export family
turns on a permission rather than on a device.

Reversing the grid changes its **kind**, not just its sign. An importing grid
is the largest source in the house; the same grid exporting is a sink, and the
diagram moves it to the other side accordingly. Everything downstream follows
from that one reclassification: the export channel takes a share of gross
power, the compensation is credited to whichever sources fed it, and
self-consumption is measured against what stayed home rather than against
everything produced.

The last snapshot is a guard rather than a scenario. The grid exports 500 W
while nothing at all is producing — a meter set that cannot physically happen,
but that a dropped sensor or a bad scaling factor will hand the engine anyway.
Gross power is exactly zero, and every ratio derived from it has to survive
being asked.
