{
  "$comment": "Property catalog for the reference-case corpus. Written once and shared by every case: it supplies the per-property reference documentation, the guiding steps for deriving a value by hand, and the dependency order to derive them in (a cost rate is far easier once the channel watts underneath it are settled). Definitions are taken from the engine's own docstrings; formulas are only stated where they have been checked against a real published value.",

  "layers": {
    "1": "Readings and totals",
    "2": "Source provenance",
    "3": "Channel split and per-source attribution",
    "4": "The monetary model"
  },

  "properties": {
    "gross_power": {
      "title": "Gross power",
      "unit": "W",
      "layer": 1,
      "definition": "Total power entering the system this snapshot — grid import plus PV production plus battery discharge. Equal to the sum of the source-adapter readings.",
      "formula": "gross_power = Σ reading(a) for every adapter a whose reading is positive",
      "depends_on": [],
      "answer_shape": "scalar",
      "derivation_steps": [
        "Classify every adapter by the sign of its reading: positive is a source, negative a sink, exactly zero is idle and belongs to neither.",
        "Sum the readings of the sources."
      ],
      "note": "Returns nothing at all if any grid / PV / battery sensor is unavailable — the total would otherwise silently under-count. A consumer dropping out does not affect it."
    },

    "combined_grid_import": {
      "title": "Grid import",
      "unit": "W",
      "layer": 1,
      "definition": "Power drawn from the grid. Zero whenever the grid is exporting.",
      "formula": "combined_grid_import = max(reading(grid), 0)",
      "depends_on": [],
      "answer_shape": "scalar",
      "derivation_steps": ["Read the grid. A positive reading is the import; a negative one means zero."]
    },

    "combined_grid_export": {
      "title": "Grid export",
      "unit": "W",
      "layer": 1,
      "definition": "Power fed to the grid, unsigned. Zero whenever the grid is importing.",
      "formula": "combined_grid_export = max(-reading(grid), 0)",
      "depends_on": [],
      "answer_shape": "scalar",
      "derivation_steps": ["Read the grid. A negative reading is the export, written positive; a positive one means zero."]
    },

    "combined_production": {
      "title": "PV production",
      "unit": "W",
      "layer": 1,
      "definition": "Total power generated by the PV adapters. A PV string drawing standby power contributes nothing here — its draw belongs to standby.",
      "formula": "combined_production = Σ max(reading(pv), 0) over the PV adapters",
      "depends_on": [],
      "answer_shape": "scalar",
      "derivation_steps": ["Sum the PV readings that are positive. Ignore any that are negative."]
    },

    "combined_charging_power": {
      "title": "Charging power",
      "unit": "W",
      "layer": 1,
      "definition": "The batteries' sink-side draw: each battery's unsigned charging power, summed. This is the CHG channel total.",
      "formula": "combined_charging_power = Σ max(-reading(battery), 0)",
      "depends_on": [],
      "answer_shape": "scalar",
      "derivation_steps": ["Sum the battery readings that are negative, written positive."]
    },

    "combined_discharging_power": {
      "title": "Discharging power",
      "unit": "W",
      "layer": 1,
      "definition": "The batteries' source-side output: each battery's unsigned discharge power, summed.",
      "formula": "combined_discharging_power = Σ max(reading(battery), 0)",
      "depends_on": [],
      "answer_shape": "scalar",
      "derivation_steps": ["Sum the battery readings that are positive."]
    },

    "combined_standby_power": {
      "title": "Standby power",
      "unit": "W",
      "layer": 1,
      "definition": "Total standby power drawn by the PV adapters — a PV string consuming rather than producing. This is the STB channel total.",
      "formula": "combined_standby_power = Σ max(-reading(pv), 0)",
      "depends_on": [],
      "answer_shape": "scalar",
      "derivation_steps": ["Sum the PV readings that are negative, written positive."]
    },

    "combined_consumption": {
      "title": "Self-consumption",
      "unit": "W",
      "layer": 1,
      "definition": "The CON channel: what is left of gross power once export, charging and standby are taken out. Computed as a residual rather than by summing consumers, so it captures the unmetered home base load alongside the metered loads.",
      "formula": "combined_consumption = gross_power − export − charging − standby, clamped at 0",
      "depends_on": [
        "gross_power",
        "combined_grid_export",
        "combined_charging_power",
        "combined_standby_power"
      ],
      "answer_shape": "scalar",
      "derivation_steps": ["Subtract export, charging and standby from gross power."]
    },

    "home_base_load_power": {
      "title": "Home base load",
      "unit": "W",
      "layer": 1,
      "definition": "Everything consumed with no sensor on it — gross power minus the metered draw. It has no adapter, competes for power like any other sink, and is derived rather than read.",
      "formula": "home_base_load_power = gross_power − Σ metered sink draws",
      "depends_on": ["gross_power"],
      "answer_shape": "scalar",
      "derivation_steps": [
        "Add up every metered sink's draw (the negative readings, written positive).",
        "Subtract that from gross power."
      ],
      "note": "OPEN QUESTION: where a sink's restriction cannot be satisfied, the engine currently leaves that undeliverable draw in this figure, so it can exceed gross minus metered draw. See captive-battery / source_in_standby."
    },

    "sink_adapters_source_shares": {
      "title": "Source provenance",
      "unit": "share",
      "layer": 2,
      "definition": "For every drawing adapter, the fraction of its power supplied by each source. Each row sums to 1, or to 0 when every source that sink is allowed happens to be idle.",
      "formula": null,
      "depends_on": ["gross_power", "home_base_load_power"],
      "answer_shape": "nested_map_fixed_keys",
      "derivation_steps": [
        "List the sources and their output, and the sinks and their draw. Remember the home base load is a sink.",
        "For each sink, note which sources it is allowed to draw from. An empty restriction means the whole mix.",
        "Find any group of sinks whose combined draw exhausts every source it is allowed — such a group has no freedom and must be served first.",
        "A restricted sink allowed the grid takes the grid before local generation.",
        "Share what remains between the flexible sinks in proportion to their draw.",
        "Express each sink's intake as fractions of its own draw."
      ],
      "note": "Two guarantees hold in every snapshot: the watts attributed to a source across all sinks equal its reading, and a restriction is only ever broken when no allocation could have honoured every restriction at once."
    },

    "home_base_load_source_shares": {
      "title": "Home base load provenance",
      "unit": "share",
      "layer": 2,
      "definition": "The home base load's own provenance row — the fraction of the unmetered draw supplied by each source.",
      "formula": null,
      "depends_on": ["sink_adapters_source_shares", "home_base_load_power"],
      "answer_shape": "map_fixed_keys",
      "derivation_steps": [
        "The home load is unrestricted, so it draws from whatever the restricted sinks leave.",
        "Express its intake as fractions of its own draw."
      ]
    },

    "gross_power_export_ratio": {
      "title": "Export ratio",
      "unit": "ratio",
      "layer": 3,
      "definition": "The fraction of gross power returned to the grid.",
      "formula": "combined_grid_export / gross_power",
      "depends_on": ["gross_power", "combined_grid_export"],
      "answer_shape": "scalar",
      "derivation_steps": ["Divide export by gross power. If gross power is zero the answer is zero, not undefined."]
    },

    "gross_power_consumption_ratio": {
      "title": "Self-consumption ratio",
      "unit": "ratio",
      "layer": 3,
      "definition": "The fraction of gross power self-consumed.",
      "formula": "combined_consumption / gross_power",
      "depends_on": ["gross_power", "combined_consumption"],
      "answer_shape": "scalar",
      "derivation_steps": ["Divide self-consumption by gross power."]
    },

    "gross_power_charging_ratio": {
      "title": "Charging ratio",
      "unit": "ratio",
      "layer": 3,
      "definition": "The fraction of gross power charged into storage.",
      "formula": "combined_charging_power / gross_power",
      "depends_on": ["gross_power", "combined_charging_power"],
      "answer_shape": "scalar",
      "derivation_steps": ["Divide charging by gross power."]
    },

    "gross_power_standby_ratio": {
      "title": "Standby ratio",
      "unit": "ratio",
      "layer": 3,
      "definition": "The fraction of gross power lost to adapter standby draw.",
      "formula": "combined_standby_power / gross_power",
      "depends_on": ["gross_power", "combined_standby_power"],
      "answer_shape": "scalar",
      "derivation_steps": ["Divide standby by gross power."]
    },

    "source_adapters_consumption_power": {
      "title": "Self-consumed power per source",
      "unit": "W",
      "layer": 3,
      "definition": "Watts of each source's output that was self-consumed — the CON channel, per source. The home base load is served through this channel like any other consumer, so for a grid-only house the whole draw shows up here against the grid.",
      "formula": null,
      "depends_on": ["sink_adapters_source_shares", "combined_consumption"],
      "answer_shape": "map_fixed_keys",
      "derivation_steps": [
        "Take the consumption channel — the metered loads plus the home base load — as sinks with their own provenance rows.",
        "Sum, per source, the watts those rows drew from it."
      ],
      "note": "Returns nothing at all (None) when gross power is unavailable, mirroring the other per-source channel splits."
    },

    "source_adapters_export_power": {
      "title": "Exported power per source",
      "unit": "W",
      "layer": 3,
      "definition": "Watts of each source's output that left through the grid. An exporting grid is a restricted sink — only devices configured to export may feed it — so a device that may not export reads zero here however much it is producing.",
      "formula": null,
      "depends_on": ["sink_adapters_source_shares", "combined_grid_export"],
      "answer_shape": "map_fixed_keys",
      "derivation_steps": [
        "The exporting grid is a sink, so its provenance row is the export mix.",
        "Multiply each source's share of that row by the exported watts."
      ]
    },

    "source_adapters_export_shares": {
      "title": "Export share per source",
      "unit": "share",
      "layer": 3,
      "definition": "Each source's share of the total exported power.",
      "formula": "source_adapters_export_power[uid] / combined_grid_export",
      "depends_on": ["source_adapters_export_power", "combined_grid_export"],
      "answer_shape": "map_fixed_keys",
      "derivation_steps": ["Divide each source's exported watts by the total export."]
    },

    "source_adapters_standby_power": {
      "title": "Standby power per source",
      "unit": "W",
      "layer": 3,
      "definition": "Watts of each source's output that went to another device's standby draw. Standby is routed through the provenance allocation like any other sink, not attributed by gross share.",
      "formula": null,
      "depends_on": ["sink_adapters_source_shares", "combined_standby_power"],
      "answer_shape": "map_fixed_keys",
      "derivation_steps": [
        "A PV string in standby is a sink with its own provenance row.",
        "Multiply each source's share of that row by the standby watts."
      ]
    },

    "combined_coe_rate": {
      "title": "Cost of electricity rate",
      "unit": "EUR/h",
      "layer": 4,
      "definition": "The marginal cost of the power entering the house right now: only the grid contribution is priced, because local generation has no marginal fuel cost.",
      "formula": "combined_coe_rate = (combined_grid_import / 1000) × grid price",
      "depends_on": ["combined_grid_import"],
      "answer_shape": "scalar",
      "derivation_steps": ["Convert the grid import to kW and multiply by the price."]
    },

    "combined_lcoe_rate": {
      "title": "Levelized cost of electricity rate",
      "unit": "EUR/h",
      "layer": 4,
      "definition": "As the marginal rate, but each local source additionally carries its own levelized cost — a PV string its LCOE, a discharging battery its LCOS.",
      "formula": "combined_lcoe_rate = Σ (output(source) / 1000) × price(source), where the grid's price is the tariff and a local source's is its own LCOE / LCOS",
      "depends_on": ["gross_power", "combined_coe_rate"],
      "answer_shape": "scalar",
      "derivation_steps": [
        "For every source, take its output in kW.",
        "Multiply by the grid tariff for the grid, or by that device's own LCOE / LCOS for local generation.",
        "Sum them."
      ]
    },

    "combined_avoided_cost_rate": {
      "title": "Avoided cost rate",
      "unit": "EUR/h",
      "layer": 4,
      "definition": "What the local generation feeding the house saved against buying the same energy from the grid.",
      "formula": "combined_avoided_cost_rate = (locally supplied share of the CON channel / 1000) × grid price",
      "depends_on": ["combined_consumption", "sink_adapters_source_shares"],
      "answer_shape": "scalar",
      "derivation_steps": [
        "Take the self-consumption channel — the metered loads plus the home base load.",
        "Work out how much of it came from local generation rather than the grid.",
        "Price those kW at the grid tariff."
      ],
      "note": "Published from both ends. The source side and the sink side measure the same euro, so they must never be added together."
    },

    "combined_saving_rate": {
      "title": "Cost saving rate",
      "unit": "EUR/h",
      "layer": 4,
      "definition": "The net marginal saving: what local generation earned by displacing an import, less what was spent importing power into storage.",
      "formula": null,
      "depends_on": ["combined_avoided_cost_rate", "sink_adapters_source_shares"],
      "answer_shape": "scalar",
      "derivation_steps": [
        "Credit each local source for the power it put into self-consumption, at the grid tariff.",
        "Debit each charging battery for the grid power it took in, at the same tariff.",
        "Add them up; the result can be negative."
      ]
    },

    "combined_export_compensation_rate": {
      "title": "Export compensation rate",
      "unit": "EUR/h",
      "layer": 4,
      "definition": "What the exported power earns, at each exporting device's own feed-in tariff. A device that may not export earns nothing.",
      "formula": "Σ (source_adapters_export_power[uid] / 1000) × export_compensation(uid)",
      "depends_on": ["source_adapters_export_power"],
      "answer_shape": "scalar",
      "derivation_steps": [
        "For each source, take the watts it exported.",
        "Multiply by that device's export compensation and sum."
      ]
    },

    "source_adapters_dynamic_coe": {
      "title": "Marginal price per source",
      "unit": "EUR/kWh",
      "layer": 4,
      "definition": "The marginal price of what each source is delivering: the tariff for the grid, and zero for local generation, whose fuel is free.",
      "formula": null,
      "depends_on": [],
      "answer_shape": "map_fixed_keys",
      "derivation_steps": [
        "The grid costs its tariff.",
        "PV and batteries read zero — a discharging battery's energy was paid for when it was charged."
      ],
      "note": "A battery appears here only while discharging, and the mix it charged on happened earlier, which a snapshot engine cannot see."
    },

    "source_adapters_dynamic_lcoe": {
      "title": "Levelized price per source",
      "unit": "EUR/kWh",
      "layer": 4,
      "definition": "As the marginal price, but each local source carries its own levelized cost instead of zero.",
      "formula": null,
      "depends_on": ["source_adapters_dynamic_coe"],
      "answer_shape": "map_fixed_keys",
      "derivation_steps": [
        "The grid costs its tariff.",
        "A PV string costs its configured LCOE; a discharging battery its flat LCOS."
      ]
    }
  }
}
